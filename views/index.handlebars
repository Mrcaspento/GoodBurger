<h1>GoodBurgers</h1>

<h3>Welcome to GoodBurger How may I take your order?</h2>


<h2>Burgers to eat!</h2>
<ul>
    {{#each burgers}}
        
    {{/each}}
</ul>

<h2>Burgers currently going through your large and small intestintes !</h2>

<ul>
    {{#each burgers}}
        {{#if devoured}}
            {{ burgers devoured = false}}
        {{/if}}
    {{/each}}
</ul>
<li>
	{{name}}

	<button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}Order Burger{{else}}{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">Eat Burger</button>
</li>
<h2>Add a burger</h2>
<form class= "create-form">

 <div class="form-group">
    <label for="bu">Burger Name:</label>
    <input type="text" id="bu" name="burger_name">
 </div>

 <div class="form-group">
     <label for="devo">Devoured?</label><br>
     <input type="radio" name="devoured" value="1" checked>Devoured!<br>
     <input type="radio" name="devoured" value="0">Not Devoured!
 </div>
 <button type="submit">Add Burger</button>
</form>