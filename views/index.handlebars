<div class="container">
  <h1>WELCOME TO GOODBURGER</h1>
  <div class="row" id="burgerBox">

    <div class="col-md-8">
      <h3>BURRRGERS</h3>
      <ul>
        {{#each burgers}}
        {{#unless devoured}}
        {{burgers}}
        {{/unless}}
        {{/each}}
      </ul>
    </div>
    <div class="col-md-4">
      <h3>Burgers going through you digestive track</h3>
      <ul>
        {{#each burgers}}
        {{#if devoured}}
        {{burgers}}
        {{/if}}
        {{/each}}
      </ul>
    </div>
  </div>
  <li class="list1">
	{{burger_name}}

	<button class="devoured btn-secondary" data-id="{{id}}"> EAT THAT MEAT SON!</button>
</li>

<li class="list2">
	{{burger_name}}
</li>

  <h2>ADD A BURGER !</h2>
  <form class="create-form">
    <div class="form-group">
      <label for="ca">Burger name:</label>
      <input type="text" id="ca" name="burger_name">
    </div>
    <button class="btn-primary" type="submit">Add Burger</button>
  </form>
</div>